extend type Query {

    "Получение одного пользователя"
    user(id: ID! @eq): user_result! @find
}

extend type Mutation {

    "Обновление профиля пользователя"
    update_profile(
        "О себе"
        bio: String
    ): profile_result
}